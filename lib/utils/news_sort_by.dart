enum NewsSortBy {
  // articles more closely related to q come first.
  relevancy,
  // default: newest articles come first.
  publishedAt,
  // articles from popular sources and publishers come first
  popularity,
}
